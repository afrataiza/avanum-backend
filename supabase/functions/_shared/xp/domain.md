# Contrato inicial do domínio de XP

O domínio separa o saldo acumulado (`XPBalance`) do histórico imutável de concessões (`XPTransaction`).

Cada transação possui valor positivo, origem e chave de idempotência. A concessão de XP deve ser centralizada em serviço de domínio nas próximas etapas.
